# MiningDiscovery — React + TypeScript + Tailwind

Scaffolded Vite React + TypeScript project converted from the provided `index.html`.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

Notes:
- Tailwind is configured in `tailwind.config.cjs` and styles are in `src/index.css`.
- The main React component is `src/App.tsx` (TypeScript + Tailwind).
